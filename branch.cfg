cfgs:
  default:
    branches: ['master']
  release-branches:
    branches: ['release-.*']
    inherit:
      terraformer:
        jobs:
          release:
            traits:
              release:
                nextversion: 'bump_patch'
